# Combined runtime requirements; modules retain their standalone dependency lists.
-r module2/requirements.txt
-r module3/requirements.txt
# Shared navigation/upload integration checks use the current testing API.
streamlit>=1.64,<2
